import { IConfig } from '@/types/interfaces';

const config: IConfig = {
   site_name: 'skele',
   canonical: 'https://skele.vercel.app/',
   title: 'skele',
   description: 'Universally fast key-first authentication for the modern web.',
   locale: 'en',
   images: [],
};

export default config;
